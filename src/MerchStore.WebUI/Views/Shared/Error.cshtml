@model MerchStore.WebUI.Models.ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<h2 class="text-danger">Ett fel inträffade när din begäran bearbetades.</h2>

@if (Model?.ShowRequestId == true)
{
    <p>Request ID: <code>@Model.RequestId</code></p>
}

@if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
{
    <div class="alert alert-danger">
        <h4>Felmeddelande:</h4>
        <p>@ViewBag.ErrorMessage</p>
    </div>
}

@if (!string.IsNullOrEmpty(ViewBag.ErrorDetails) && Context.Request.Host.Host == "localhost")
{
    <div class="card mt-4">
        <div class="card-header bg-danger text-white">
            <h4 class="mb-0">Tekniska detaljer</h4>
        </div>
        <div class="card-body">
            <pre><code>@ViewBag.ErrorDetails</code></pre>
        </div>
    </div>
}

<h3>Utvecklingsläge</h3>
<p>
    Att byta till <strong>Development</strong>-miljö visar mer detaljerad information om felet som inträffat.
</p>
<p>
    <strong>Development-miljön bör inte aktiveras för publicerade applikationer.</strong>
    Det kan leda till att känslig information från undantag visas för slutanvändare.
    För lokal felsökning, aktivera <strong>Development</strong>-miljön genom att sätta miljövariabeln <strong>ASPNETCORE_ENVIRONMENT</strong> till <strong>Development</strong>
    och starta om appen.
</p>